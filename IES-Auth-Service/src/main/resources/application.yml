spring:
  application:
    name: IES-AUTH-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/ies
    username: root
    password: root@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 4444
